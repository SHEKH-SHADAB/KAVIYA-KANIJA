package com.example.kavyakanija.ui.theme

import androidx.compose.ui.graphics.Color

// Sophisticated Palette
val EmeraldGreen = Color(0xFF004D40)
val DeepTeal = Color(0xFF00695C)
val GoldAccent = Color(0xFFFFD700)
val SoftEmerald = Color(0xFF80CBC4)

// Premium Accents
val Platinum = Color(0xFFE5E4E2)
val Charcoal = Color(0xFF36454F)
val RichBlack = Color(0xFF010B13)

// Backgrounds
val BgLight = Color(0xFFF1F8E9)
val BgDark = Color(0xFF00241F)

// Glassmorphism helpers
val GlassWhite = Color.White.copy(alpha = 0.1f)
val GlassStroke = Color.White.copy(alpha = 0.2f)

// Category Colors (Vibrant but balanced)
val CatPhil = Color(0xFFE91E63) // Pink
val CatNature = Color(0xFF4CAF50) // Green
val CatPatriotic = Color(0xFFFF9800) // Orange
val CatClassic = Color(0xFF673AB7) // Purple
val CatModern = Color(0xFF03A9F4) // Blue

// Gradients
val PremiumGradientStart = Color(0xFF004D40)
val PremiumGradientEnd = Color(0xFF00897B)

val WarmGradientStart = Color(0xFFFF5F6D)
val WarmGradientEnd = Color(0xFFFFC371)

val SoftGradientStart = Color(0xFFFFF1EB)
val SoftGradientEnd = Color(0xFFACE0F9)

// Accent Colors
val SilverAccent = Color(0xFFC0C0C0)
